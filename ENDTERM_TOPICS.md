# 42 темы проектов (недели 9–14)

Связность: одна сквозная задача на проект; данные не меняются между неделями без версии.  
ответы API — JSON и/или файлы (CSV, PNG, ZIP). 
ссылка на выбор темы https://docs.google.com/spreadsheets/d/188PrVIjTqiwlMIEy-E1vPsqAv9SYGpDb/edit?usp=sharing&ouid=109431966877820439256&rtpof=true&sd=true

---

## 1. Экзаменационная ведомость

**Сквозная задача:** файл `exam.csv`: колонки `group`, `score` (и при необходимости ещё одна числовая). Сводка по группам и вывод тех же чисел через API.

- **9:** Загрузить `score` в `ndarray`, посчитать глобальное среднее и стандартное отклонение (или размах min–max).
- **10:** На том же столбце — квантили 0.25 и 0.75 через `np.percentile`, без Pandas.
- **11:** Полный `read_csv`: типы, `describe()`, число пропусков в `score`.
- **12:** `groupby('group')`: средний балл и число студентов в группе; сохранить `summary_groups.csv`.
- **13:** Гистограмма всех `score` и bar средних по группам (два подграфика или два PNG — по договорённости курса).
- **14:** `POST` тот же CSV → JSON: глобальное среднее и таблица «группа → средний балл» как в н.12.

---

## 2. Журнал входов в лабораторию

**Сквозная задача:** `lab_log.csv`: строка времени `ts` и флаг `student_id`. Посчитать, сколько входов в каждый час дня (упрощённо — без микросекунд).

- **9:** Прочитать колонку времени в строки, преобразовать в число «час суток» (0–23) в массив (парсинг через срезы строки `HH` или `datetime`).
- **10:** На том же массиве часов — `np.bincount` или цикл по 24 бинам: сколько входов в каждом часе.
- **11:** DataFrame с колонками `ts`, `hour`, `student_id`; отфильтровать один учебный день по дате (строка даты).
- **12:** Для отфильтрованного дня — таблица «час → число входов», сохранить `entries_by_hour.csv`.
- **13:** Столбчатый график входов по часам за выбранный день; PNG.
- **14:** API: параметр даты (строка) → JSON той же таблицы, что в н.12.

---

## 3. Генератор фиктивных оценок

**Сквозная задача:** один скрипт с `seed` создаёт `fake_grades.csv` (студент, предмет, оценка); дальше все недели используют этот файл или его обновлённую версию с теми же колонками.

- **9:** Задать `rng = np.random.default_rng(seed)`, сгенерировать N строк: `student_id`, `subject_id`, `grade` (целые 2–5 или float).
- **10:** Добавить к генератору столбец «шум» или вторую оценку с корреляцией (например вторая = 0.7×первая + шум), сохранить новую версию файла.
- **11:** Загрузить последний CSV в Pandas, проверить диапазон оценок и типы.
- **12:** `groupby` по `subject_id`: средняя оценка; отсортировать предметы по убыванию среднего.
- **13:** Boxplot или violin по нескольким предметам (до 5 штук) или гистограмма оценок; PNG.
- **14:** GET `?seed=&n_rows=` → JSON метаданных и путь/имя сгенерированного файла (логика как в н.9–11).

---

## 4. Проверка баллов 0–100

**Сквозная задача:** `marks.csv`, колонка `points`. Правило: значение от 0 до 100 включительно, без NaN.

- **9:** Загрузить `points` в массив, найти индексы, где `NaN` или вне [0,100] (маски в NumPy).
- **10:** На том же массиве — посчитать, сколько строк «хороших» и сколько «плохих».
- **11:** DataFrame с колонкой `ok` (True/False) по правилу; вывести первые 10 нарушений.
- **12:** Сохранить `marks_clean.csv` только с валидными строками (или с флагом — по выбору, но согласованно с н.14).
- **13:** Bar: число валидных vs невалидных; PNG.
- **14:** POST CSV → JSON: `valid_count`, `invalid_count`, список индексов первых 20 ошибок (как в н.11).

---

## 5. Среднее по частям файла

**Сквозная задача:** один файл `big.csv` (можно 10–50 тыс. строк), одна числовая колонка `x`. Сравнить среднее при чтении чанками и при чтении всего столбца (на подмножестве строк).

- **9:** Читать CSV чанками (например по 2000 строк), накапливать сумму и счётчик для `x` в NumPy.
- **10:** То же чтение, но сменить `float64` на `float32` для колонки — сравнить финальное среднее с н.9 на одном файле.
- **11:** Прочитать только первые 5000 строк целиком через Pandas, посчитать среднее `x` как эталон для малого префикса.
- **12:** Таблица из трёх строк: метод (чанки / float32 / prefix), среднее значение.
- **13:** Простой график: по оси X — номер «шага» (накопленное число строк), по Y — текущее среднее при чанковом чтении; PNG.
- **14:** GET параметры `chunk_size` → JSON финального среднего и числа обработанных строк (как в н.9–12).

---

## 6. Рост и масса (одна прямая)

**Сквозная задача:** `body.csv`: колонки `height_cm`, `weight_kg`. Линейная аппроксимация веса от роста (без sklearn).

- **9:** Два массива, убрать строки с NaN вручную по маске; scatter в виде двух массивов для дальнейшего.
- **10:** Построить матрицу с колонкой единиц и `height_cm`, решить МНК через `np.linalg.lstsq` для `weight_kg`; сохранить коэффициенты в переменные.
- **11:** DataFrame, колонка `pred_weight` = a×height + b из н.10.
- **12:** Посчитать MAE между `weight_kg` и `pred_weight` одной формулой в NumPy; записать в `metrics.txt` или dict.
- **13:** Scatter рост–вес и линия регрессии; PNG.
- **14:** POST CSV → JSON коэффициентов и MAE (как в н.10–12).

---

## 7. Опрос: ответы «да / нет»

**Сквозная задача:** `poll.csv`: колонки вопросов со значениями `yes`/`no` (строки). Перевести в 0/1 и посчитать доли «да» по каждому вопросу.

- **9:** Для одной колонки-вопроса заменить строки на числа через словарь и массив; посчитать долю единиц.
- **10:** То же для всех вопросов в цикле по именам колонок, результат — список или массив долей.
- **11:** DataFrame из 0/1, `describe()` по каждому вопросу.
- **12:** Если есть колонка `city`, сделать `groupby('city')` и среднее по одному выбранному вопросу; сохранить `by_city.csv`.
- **13:** Bar долей «да» по вопросам (по оси X — имена вопросов); PNG.
- **14:** POST CSV → JSON словаря «имя вопроса → доля да» плюс при наличии города — таблица из н.12.

---

## 8. Заказы и города клиентов

**Сквозная задача:** `orders.csv` (`customer_id`, `amount`) и `customers.csv` (`customer_id`, `city`). Выручка по городу после объединения.

- **9:** Загрузить `customer_id` из обоих файлов в массивы; проверить типы (строка vs int — привести к одному виду).
- **10:** Убедиться, что в `customers` нет дубликатов `customer_id` (множество или `value_counts`).
- **11:** `merge(orders, customers, on='customer_id', how='left')`, посчитать число строк без города (NaN).
- **12:** `groupby('city')['amount'].sum()`, топ-5 городов, сохранить `revenue_by_city.csv`.
- **13:** Bar топ-5 городов по выручке; PNG.
- **14:** POST два файла → JSON топ-5 и общей суммы (как в н.12).

---

## 9. Цены с запятой

**Сквозная задача:** `prices_raw.csv`, колонка `price` как строка вида `"12,5"` или `"1 234,56"`. Привести к float и сохранить чистый файл.

- **9:** Загрузить столбец как массив строк, найти строки, где нет цифр (маска плохих).
- **10:** Функция: убрать пробелы, заменить запятую на точку, `astype(float)` для валидных строк.
- **11:** DataFrame с колонками `price_raw`, `price_clean`, флаг ошибки парсинга.
- **12:** Сохранить `prices_clean.csv` с числовой колонкой `price_clean` и исходными остальными колонками.
- **13:** Гистограмма `price_clean` до и после (или одна гистограмма после очистки); PNG.
- **14:** POST файл → `FileResponse` `prices_clean.csv` или JSON числа исправленных/ошибочных строк.

---

## 10. День недели из даты события

**Сквозная задача:** `events.csv`: колонка `event_time` (строка ISO или `YYYY-MM-DD HH:MM`). Добавить `weekday` (0=Пн … 6=Вс) и посчитать активность по дням недели.

- **9:** Из строки даты извлечь дату без времени, перевести в ordinal или в tuple (год, месяц, день) для сортировки.
- **10:** Вычислить номер дня недели формулой или через `datetime` в цикле по массиву (упрощённо — допустим цикл для N<5000).
- **11:** Pandas: `pd.to_datetime`, колонка `weekday`, исходная таблица не меняет смысл данных.
- **12:** `groupby('weekday').size()` — число событий по дням недели; сохранить `counts_weekday.csv`.
- **13:** Bar по 7 дням; PNG.
- **14:** GET → JSON таблицы из н.12 (список пар день–число).

---

## 11. Несколько однотипных отчётов

**Сквозная задача:** в папке лежат `report_jan.csv`, `report_feb.csv`, … с одинаковыми колонками `item`, `qty`. Общая сумма `qty` и сумма по каждому файлу.

- **9:** Список путей к файлам (`glob` или ручной список), для каждого прочитать только колонку `qty` в массив и сумму.
- **10:** Накопить в NumPy вектор «сумма по файлу» для всех файлов.
- **11:** Собрать DataFrame: колонки `month`, `total_qty` (имя месяца из имени файла).
- **12:** Общая сумма `qty` по всем файлам одной строкой; добавить в отчёт `summary_all.csv`.
- **13:** Bar: вклад каждого месяца в общую сумму; PNG.
- **14:** POST список имён файлов (JSON) → JSON `totals` по месяцам и `grand_total` (как в н.11–12).

---

## 12. Оценки по месяцам в одну колонку

**Сквозная задача:** `wide_scores.csv`: строки — студент, столбцы `jan`, `feb`, `mar`, … с баллами. Превратить в длинный формат: `student`, `month`, `score`.

- **9:** Прочитать заголовок, список месяцев как список строк.
- **10:** Числовой блок (без имени студента) в матрицу NumPy, проверить форму.
- **11:** `melt` с `id_vars=['student']`, `var_name='month'`, `value_name='score'`.
- **12:** `pivot` обратно и сравнить одну ячейку с исходным `wide_scores.csv` (должно совпасть).
- **13:** Линейный график баллов по месяцам для **одного** выбранного студента из long-таблицы; PNG.
- **14:** POST wide CSV → JSON long (список записей) как в н.11.

---

## 13. Код дисциплины и название

**Сквозная задача:** `enroll.csv` (`course_code`, `student_id`) и `courses.csv` (`course_code`, `title`). После merge — сколько студентов на каждом **названии** курса.

- **9:** Массивы кодов из обоих файлов; пересечение множеств «какие коды есть везде».
- **10:** Отсортировать коды, подготовить к объединению без дубликатов в справочнике.
- **11:** `merge` enroll с courses по `course_code`.
- **12:** `groupby('title')['student_id'].nunique()` — число уникальных студентов на курсе; `top_courses.csv`.
- **13:** Горизонтальный bar топ-7 курсов по числу студентов; PNG.
- **14:** POST два CSV → JSON топ курсов (как в н.12).

---

## 14. Два варианта сохранения одной таблицы

**Сквозная задача:** после `groupby` получена таблица `result` (небольшая). Сохранить её как CSV с `;` и с `,`, сравнить размер файла в байтах.

- **9:** Таблицу агрегатов получить из NumPy (сводка в матрицу) или из Pandas — зафиксировать одну итоговую `result`.
- **10:** `result.to_csv('out_comma.csv', sep=',')` и `to_csv('out_semi.csv', sep=';')`, записать размеры файлов в dict.
- **11:** Округлить числовые колонки до 2 знаков при сохранении третьего файла `out_round.csv`.
- **12:** Таблица-отчёт: три строки (вариант экспорта, размер в байтах).
- **13:** Bar трёх размеров; PNG.
- **14:** GET `?sep=comma|semi` → `FileResponse` соответствующего CSV с **тем же** содержимым `result`.

---

## 15. Ошибка прогноза в процентах

**Сквозная задача:** `forecast.csv`: колонки `y_true`, `y_pred`. Считать MAPE (среднее |(y_true−y_pred)/y_true|), пропуская строки где `y_true==0`.

- **9:** Два массива, маска «y_true не ноль», отфильтровать пары значений.
- **10:** Вектор относительных ошибок в процентах, среднее в NumPy.
- **11:** DataFrame с колонкой `ape` (абсолютная процентная ошибка по строке).
- **12:** MAPE по группе, если есть колонка `group` (один `groupby`); иначе одно число MAPE — сохранить в `mape_result.json` или dict.
- **13:** Scatter `y_true` vs `y_pred` и линия идеала y=x; PNG.
- **14:** POST CSV → JSON `mape` и при наличии групп — словарь по группам (как в н.12).

---

## 16. Три ряда и тепловая карта корреляций

**Сквозная задача:** `triple.csv` — ровно три числовых столбца `a`, `b`, `c`. Корреляционная матрица 3×3 и визуализация.

- **9:** Матрица N×3 в `ndarray`, вычесть среднее по столбцам (центрирование вручную).
- **10:** Ковариация и корреляция между двумя выбранными столбцами формулой (например a и b).
- **11:** `df[['a','b','c']].corr()` — эталон; сравнить одну ячейку с н.10.
- **12:** Без rolling — только статика; сохранить `corr_matrix.csv`.
- **13:** `seaborn.heatmap` или `imshow` матрицы 3×3 с подписями; PNG.
- **14:** GET/POST файл → JSON матрицы 3×3 (как в н.11–12).

---

## 17. Три кластера на плоскости

**Сквозная задача:** `points.csv` с колонками `x`, `y`. Фиксированное **k=3**, без подбора k: несколько итераций k-means вручную на NumPy.

- **9:** Загрузить координаты в массив N×2, выбрать три стартовых центра (первые три точки или случайно с seed).
- **10:** Цикл: назначить каждой точке ближайший центр (расстояние по евклиду), пересчитать центры как среднее точек кластера; повторить 10 раз.
- **11:** DataFrame с колонками `x`, `y`, `cluster_id` (0,1,2).
- **12:** Таблица центров (3 строки), сохранить `centers.csv`.
- **13:** Scatter: цвет по `cluster_id`, маркеры центров крупнее; PNG.
- **14:** POST JSON массив точек → JSON центров и списка меток (как в н.11–12).

---

## 18. Две главные оси (упрощённо)

**Сквозная задача:** таблица `iris_like.csv` — три числовых признака (без sklearn). Центрировать, посчитать ковариацию 3×3, два собственных вектора для проекции на плоскость.

- **9:** Матрица n×3, вычесть среднее по каждому столбцу.
- **10:** Ковариационная матрица 3×3 = (X.T @ X) / (n−1), `np.linalg.eigh` — собственные векторы и значения.
- **11:** Взять два вектора с наибольшими собственными значениями, построить столбцы `pc1`, `pc2` = X @ V[:, :2].
- **12:** Доля дисперсии: λᵢ / сумма λ; записать в Series или маленький CSV.
- **13:** Scatter `pc1` vs `pc2`; PNG.
- **14:** POST CSV с тремя числовыми колонками → JSON первых двух компонент (списки `pc1`, `pc2`) и долей дисперсии (как в н.12).

---

## 19. Две группы: доля успехов

**Сквозная задача:** файл `ab.csv`: колонки `variant` (A/B) и `success` (0/1). Всё про сравнение долей в одной таблице.

- **9:** Разбить `success` на два массива по маске A и B, посчитать сумму и длину в NumPy.
- **10:** На тех же данных — доля успехов для A и для B векторно (без цикла по строкам, если получается через маски).
- **11:** Тот же файл: `groupby('variant')` — среднее по `success` и количество строк.
- **12:** На той же таблице — добавить, например, разбивку по одному дополнительному столбцу (день недели) или отсортировать и сохранить маленькую сводку в CSV.
- **13:** Столбчатый график двух долей (A и B); PNG.
- **14:** API: загрузка того же формата → JSON с двумя долями и числом наблюдений (как в н.11).

---

## 20. Train / Test одним разрезом

**Сквозная задача:** один `data.csv` с признаками и колонкой `y`. Один разрез 80/20 с фиксированным `random_state`.

- **9:** Загрузить `y` и один признак в массивы, перемешать индексы в NumPy (`rng.permutation`).
- **10:** Построить маски train/test для **той же длины**, сохранить индексы в два файла или переменные для следующих недель.
- **11:** Pandas: `train` и `test` DataFrame, `to_csv('train.csv')`, `to_csv('test.csv')`.
- **12:** На тех же частях — среднее `y` в train и в test; при необходимости одна простая метрика на train (например MAE константного прогноза).
- **13:** Две гистограммы `y` (train и test) рядом на одной фигуре; PNG.
- **14:** API: параметр `seed` (опционально) → JSON числа строк train/test и средних `y` (логика как в н.12).

---

## 21. Один ряд: лаг и скользящее

**Сквозная задача:** один столбец `value` в `series.csv`. Строим только лаг 1 и скользящее среднее с окном 3.

- **9:** Загрузить столбец в `ndarray`, сдвинуть на 1 (лаг) через срезы, вывести длину совпадающей части.
- **10:** На том же ряду — скользящее среднее окна 3 через `cumsum` трюк или простой цикл только для обучения, сравнить с Pandas на куске.
- **11:** DataFrame: колонки `value`, `lag1`, `roll3` через `shift` и `rolling`.
- **12:** На той же таблице — сохранить в `features.csv` без добавления внешних файлов (всё из одного исходника).
- **13:** Линия: исходный ряд и `roll3` на одном графике; PNG.
- **14:** API: POST список чисел или CSV → JSON последних значений `lag1` и `roll3` (как в н.11).

---

## 22. Мини-отчёт по таблице

**Сквозная задача:** один небольшой CSV (до ~1000 строк). Автоматически: число строк, список числовых колонок, простые графики.

- **9:** Программно определить, какие колонки числовые (через попытку `astype(float)` или по имени), базовые суммы в NumPy.
- **10:** Для **двух** выбранных числовых колонок — min/max/mean в массивах.
- **11:** Полный `read_csv`, `describe()`, список пропусков по колонкам (простой цикл или `isna().sum()`).
- **12:** Для одной категориальной колонки — `value_counts()` топ-5; для одной числовой — границы квартилей.
- **13:** Два PNG: гистограмма одной колонки + bar топ-5 категорий (если есть).
- **14:** `GET` с путём к файлу → JSON: `n_rows`, `describe` для двух колонок, топ категорий (как в н.11–12).

---

## 23. Продажи по дням (один файл)

**Сквозная задача:** `sales.csv`: дата (`date`), сумма (`amount`), опционально `category`. KPI: общая выручка и выручка по дням.

- **9:** Колонку `amount` в массив, общая сумма и средний чек.
- **10:** Если дата в виде строки — преобразовать в ordinal или в массив год-месяц-день для группировки в NumPy (упрощённо: один уникальный день = один индекс).
- **11:** Pandas: `pd.to_datetime`, сумма по дням `groupby` по дате.
- **12:** На тех же данных — сумма по `category` (один `groupby`) или топ-3 дня по выручке, сохранить маленькую таблицу `kpi.csv`.
- **13:** Линия выручки по дням; второй график — bar по категориям (если колонка есть); 1–2 PNG.
- **14:** API: `GET /kpi` → JSON: общая сумма, число дней, топ дня (как в н.12).

---

## 24. График по списку колонок из JSON

**Сквозная задача:** один `plot_data.csv` и маленький файл `preset.json` с полями `"x"`, `"y"`, `"kind"` (`line` или `scatter`).

- **9:** Прочитать JSON в словарь, проверить, что имена колонок есть в заголовке CSV (прочитать только заголовок).
- **10:** Загрузить две колонки в NumPy по именам из JSON.
- **11:** DataFrame, выбрать две колонки, убрать строки с NaN в этих двух.
- **12:** Обернуть построение в одну функцию `make_plot(df, preset)` — возвращает путь к PNG.
- **13:** Сохранить `out.png` с подписями осей из имён колонок.
- **14:** `POST`: тело JSON preset + файл CSV → `FileResponse` того же PNG, что при локальном запуске.

---

## 25. Выбросы по IQR (одна колонка)

**Сквозная задача:** один числовой столбец в `values.csv`. Пометить выбросы правилом IQR, те же строки — в API.

- **9:** Загрузить столбец в массив, посчитать Q1, Q3 в NumPy (`np.percentile`).
- **10:** Границы `[Q1 - 1.5*IQR, Q3 + 1.5*IQR]`, булева маска выбросов на том же массиве.
- **11:** DataFrame с колонкой `is_outlier`.
- **12:** На той же таблице — число выбросов и список индексов (первые 20 для отчёта).
- **13:** Scatter: индекс строки vs значение, цвет по `is_outlier`; PNG.
- **14:** POST CSV → JSON: `count_outliers`, `indices_sample` (как в н.12).

---

## 26. Регионы: метрика + справочник

**Сквозная задача:** `metrics.csv` (`region_id`, `score`) и `regions.csv` (`region_id`, `name`) — без карт, только таблицы.

- **9:** Два массива ключей из двух файлов, проверка, что все id из metrics есть в regions (множества).
- **10:** После сортировки — простое сопоставление или подготовка к `merge` (без SQL).
- **11:** `merge` в Pandas, итоговая таблица с именем региона.
- **12:** Сортировка по `score`, топ-5 регионов с именами, сохранить `top5.csv`.
- **13:** Bar горизонтальный для топ-5; PNG.
- **14:** API: два файла → JSON топ-5 (как в н.12).

---

## 27. Упрощённые когорты

**Сквозная задача:** мало пользователей (10–30): `events.csv` — `user_id`, `event_date`. Считаем «активность по неделям после первого события» в упрощённом виде.

- **9:** Уникальные пользователи и минимальная дата на пользователя (можно циклом по уникальным id).
- **10:** Для каждого события — номер недели относительно первой даты пользователя (целые числа в массиве или списке).
- **11:** DataFrame с колонками `user_id`, `week`, агрегат: число событий на пару (user, week) через `groupby`.
- **12:** Сводная таблица: строки — пользователь, столбцы — неделя 0,1,2 (заполнить нулями где нет данных), сохранить `cohort_matrix.csv`.
- **13:** Heatmap или таблица с цифрами (imshow); PNG.
- **14:** API: тот же `events.csv` → JSON матрицы в виде списка списков или словаря (как в н.12).

---

## 28. Три графика — три файла

**Сквозная задача:** один `report.csv`; три разных графика сохраняются как `fig1.png`, `fig2.png`, `fig3.png` и перечисляются в `manifest.json`.

- **9:** Три числа для заголовка отчёта (например mean трёх колонок) в dict.
- **10:** Три функции без копипасты осей: общий стиль через параметры.
- **11:** `describe()` в словарь для JSON.
- **12:** Список имён файлов и контрольные суммы или размеры файлов в байтах.
- **13:** Три отдельных PNG (hist, boxplot, scatter — на выбор по данным).
- **14:** `GET /manifest` → JSON из н.12; `GET /fig?n=1` → файл PNG (без HTML-страниц).

---

## 29. Оценка π (Монте-Карло)

**Сквозная задача:** точки в квадрате [-1,1]×[-1,1], доля внутри круга → оценка π. Один сценарий на все недели.

- **9:** Сгенерировать N=10⁴ пар (x,y) в NumPy, маска `x²+y² ≤ 1`, доля внутри.
- **10:** Формула `4 * доля` как оценка π; повторить для N=10⁵ на том же `seed`+следующий шаг.
- **11:** Таблица DataFrame: колонки `N`, `pi_hat` для 3–4 значений N.
- **12:** На тех же данных — разница с `math.pi` в новой колонке.
- **13:** График: по оси X — N, по оси Y — `pi_hat`; горизонтальная линия `math.pi`; PNG.
- **14:** GET `?n=` → JSON `pi_hat` и ошибки (как в н.11–12).

---

## 30. Одномерное блуждание

**Сквозная задача:** шаги +1/-1, длина трассы T=100, M=200 траекторий — одни и те же параметры.

- **9:** Матрица шагов `(M, T)` из `choice([-1,1])`, кумулятивная сумма по оси 1 — позиция.
- **10:** Среднее и дисперсия конечной позиции по M траекториям.
- **11:** DataFrame: несколько траекторий в длинном формате (опционально) или широкая матрица первых 5 путей.
- **12:** Гистограмма конечных позиций, сохранить статистику в dict.
- **13:** 3–5 линий траекторий на графике + гистограмма концов; PNG.
- **14:** GET параметры M, T → JSON средней и дисперсии конца (как в н.10–12).

---

## 31. Бутстреп среднего

**Сквозная задача:** один столбец из 50–100 чисел `sample.csv`. Бутстреп среднего, B=500, перцентили 2.5 и 97.5.

- **9:** Загрузить столбец, `rng.choice` с возвращением для одной бутстреп-выборки, среднее.
- **10:** Повторить B раз, собрать массив бутстреп-средних.
- **11:** Перцентили и исходное среднее в Series/DataFrame одной строки.
- **12:** Записать `bootstrap_summary.csv` с колонками `mean`, `lo`, `hi`.
- **13:** Гистограмма бутстреп-средних, вертикальные линии lo/hi; PNG.
- **14:** POST массив чисел → JSON `mean`, `lo`, `hi` (как в н.11–12).

---

## 32. Простой AR(1) ряд

**Сквозная задача:** модель `x[t] = a * x[t-1] + eps[t]`, фиксированные `a` и длина T. Один сценарий генерации.

- **9:** Цикл или векторная генерация `eps`, затем цикл AR(1) на 50–100 точек.
- **10:** Две реализации с разным `a` (например 0.5 и 0.9), сохранить в два столбца одного CSV.
- **11:** DataFrame с индексом времени 0..T-1.
- **12:** Среднее и std каждого столбца, таблица из двух строк.
- **13:** Две линии на одном графике; PNG.
- **14:** GET `?a=&T=` → JSON ряда (список чисел) как в н.9–11.

---

## 33. Две выборки из одной таблицы

**Сквозная задача:** `population.csv`: колонки `stratum`, `value`. Сравнить простую случайную выборку 20 строк и стратифицированную (по 10 из каждой страты, страт 2 штуки).

- **9:** Отфильтровать страты в два массива `value`, посчитать истинное среднее по всей таблице (одно число).
- **10:** Случайная выборка 20 индексов без страт, среднее выборки.
- **11:** Стратифицированно: из каждой группы `groupby` взять по 10 случайных (`sample`), объединить.
- **12:** Таблица из трёх средних: истинное, простое, стратифицированное.
- **13:** Простой bar из трёх столбцов; PNG.
- **14:** POST CSV → JSON трёх средних (как в н.12).

---

## 34. Цепь Маркова 3×3

**Сквозная задача:** задана матрица P 3×3 (стохастическая). Симуляция 500 шагов, одна цепь.

- **9:** Хранить P в `ndarray`, проверить, что строки суммируются в ~1.
- **10:** Симуляция: на каждом шаге выбрать следующее состояние по строке `P[current]`.
- **11:** Частоты посещений состояний в DataFrame одной строки.
- **12:** Умножить P на себя несколько раз, посмотреть на стационарное приближение (одна строка матрицы как вектор).
- **13:** Bar частот симуляции и горизонтальные линии стационарного приближения (если посчитано); PNG.
- **14:** POST JSON матрица P → JSON частот из симуляции (как в н.11).

---

## 35. Два генератора случайных чисел

**Сквозная задача:** сравнить время генерации массива размера 1e6: старый интерфейс `np.random` vs `np.random.default_rng()` — один эксперимент.

- **9:** Один замер времени для `np.random.rand(10**6)`.
- **10:** Второй замер для `default_rng().random(10**6)`.
- **11:** Таблица из двух строк: метод, секунды.
- **12:** Повторить 5 раз, усреднить (простой цикл), записать среднее.
- **13:** Bar два столбца — среднее время; PNG.
- **14:** GET → JSON двух средних времени (как в н.12).

---

## 36. Запросы в минуту

**Сквозная задача:** лог `requests.csv`: `timestamp`, `ip`. Посчитать число запросов в каждой минуте и топ IP за весь файл.

- **9:** Парсинг времени в число секунд или минут от начала файла (один массив).
- **10:** Гистограмма или `bincount` по номеру минуты для подсчёта запросов в минуту.
- **11:** DataFrame: `groupby` по минуте (округлённое время), размер группы.
- **12:** Отдельно `value_counts` по `ip`, топ-5.
- **13:** Линия «запросы в минуту» + при желании bar топ IP; 1–2 PNG.
- **14:** POST лог → JSON: максимум RPS (запросов в минуту), топ-3 IP.

---

## 37. Прайс и одна скидка

**Сквозная задача:** `products.csv` (`id`, `price`), фиксированная скидка 10% на всё. Корзина — список `id` в JSON.

- **9:** Цены в массив, применить множитель 0.9 векторно к выбранным индексам.
- **10:** Для списка id корзины посчитать сумму цен до и после скидки на NumPy.
- **11:** DataFrame после merge корзины с прайсом (через `map` или `merge` на маленькой таблице корзины).
- **12:** Итоговая сумма, НДС опционально не включать — одна цифра «к оплате».
- **13:** Bar цен товаров из **одной** тестовой корзины (до/после); PNG.
- **14:** POST JSON `{"ids": [...]}` → JSON `total_before`, `total_after`.

---

## 38. Остаток на складе (один склад)

**Сквозная задача:** `movements.csv`: `date`, `qty` (+ приход, − расход). Один склад, без нескольких локаций.

- **9:** Массив `qty`, кумулятивная сумма `np.cumsum`, финальный остаток.
- **10:** Проверка: сумма всех движений равна последнему остатку.
- **11:** DataFrame с колонкой `balance` = `cumsum`.
- **12:** Сохранить `balance_daily.csv` с колонками дата и остаток на конец дня (если несколько строк в день — агрегировать суммой за день, затем cumsum).
- **13:** Линия остатка по дням; PNG.
- **14:** GET → JSON текущего остатка и даты последней операции (из н.12).

---

## 39. Проверка настроек (JSON)

**Сквозная задача:** файл `settings.json` с полями `alpha` (0..1), `n_iter` (целое >0), `name` (строка непустая). Сервис только валидирует и сравнивает две версии.

- **9:** Загрузить JSON в dict, проверить наличие ключей.
- **10:** Проверить типы и диапазоны чисел в коде (без pydantic можно, с pydantic — по желанию курса).
- **11:** Список ошибок списком строк; если ок — пустой список.
- **12:** Функция `diff(a, b)` — какие поля отличаются между двумя dict.
- **13:** Bar значений числовых полей для двух конфигов рядом (если поля числовые); один PNG.
- **14:** POST два JSON → JSON `ok`/`errors` и `diff` (как в н.11–12).

---

## 40. Топ ключей кэша

**Сквозная задача:** `cache_log.csv`: одна колонка `key` (строка). Посчитать частоты и топ-10.

- **9:** Загрузить в массив строк (object), число строк.
- **10:** Уникальные значения и подсчёт через `np.unique(..., return_counts=True)` или цикл только для обучения.
- **11:** `value_counts()` в Pandas, топ-10.
- **12:** Сохранить `top10.csv` с колонками `key`, `count`.
- **13:** Горизонтальный bar топ-10; PNG.
- **14:** POST CSV → JSON списка `{key, count}` (как в н.12).

---

## 41. Длительность задач

**Сквозная задача:** `jobs.csv`: `duration_sec`, `status` (`ok`/`fail`). Перцентили только по `ok`, гистограмма.

- **9:** Массив duration, маска `status == ok`, массив длительностей успешных.
- **10:** Перцентили 50, 90, 95 в NumPy для успешных.
- **11:** DataFrame, фильтр ok, `describe()`.
- **12:** Число fail и доля fail по всем строкам.
- **13:** Гистограмма duration для ok; PNG. (ECDF не обязателен.)
- **14:** GET или POST файл → JSON перцентилей и `fail_rate` (как в н.11–12).

---

## 42. Учёт времени на задачи (мини-журнал)

**Сквозная задача:** приложение хранит в памяти таблицу записей `task_name`, `minutes`, `date` (строка). POST добавляет строки, GET возвращает сводку по **тем же** данным, что уже накоплены.

- **9:** Описать одну запись как dict с обязательными ключами; функция проверки: `minutes` > 0, `task_name` непустая строка, `date` в формате `YYYY-MM-DD`.
- **10:** Список словарей в памяти преобразовать в один `ndarray` по минутам и посчитать общую сумму минут без Pandas.
- **11:** Собрать `DataFrame` из всего списка записей, типы столбцов, отсортировать по дате.
- **12:** `groupby('task_name')['minutes'].sum()` — топ задач по времени; сохранить при локальном запуске `tasks_summary.csv`.
- **13:** Bar горизонтальный топ-5 задач по суммарным минутам; PNG.
- **14:** FastAPI: `POST /ingest` — JSON с одной или несколькими записями (добавить в общий список после валидации); `GET /summary` — JSON с `total_minutes` и таблицей «задача → сумма минут» как в н.12; опционально `GET /export` — скачивание того же `tasks_summary.csv`. Без базы данных, без SQL.
